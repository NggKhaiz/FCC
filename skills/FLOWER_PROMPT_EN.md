# Flower Prompt (English) — every chat

## Restated user request

> Continue optimizing and coding to the absolute best level. You must self-drive
> **plan → stats → build → pentest → debug → loop**. Stability first, peak quality
> second. Aim for **Rust** on the highest-throughput paths. On **every** chat:
> read the Flower skill, recreate the user prompt as **XML + English** so the
> problem is unambiguous, then execute.

## Canonical skill

- XML: `skills/Fl0w3r_RHLZ_V3r$!0n_03.xml`
- This file: English companion

## Non-negotiables

1. Real git history only — no fictional Raven bundles.
2. Native Rust is **optional**; Python ultra-core always ships.
3. Security posture never regresses for speed.
4. Work stays on `arena/01a095da-fcc`.

## Current North Star (Phase 4)

Ship `fcc_core` (Rust source) + `free_claude_code.native.ultra` (Python) wired into
validation, rate limiting, metrics, token approx, and admin UI paint performance.
