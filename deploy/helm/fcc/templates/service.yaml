apiVersion: v1
kind: Service
metadata:
  name: {{ include "fcc.fullname" . }}
  labels:
    app.kubernetes.io/name: fcc
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: fcc
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http
